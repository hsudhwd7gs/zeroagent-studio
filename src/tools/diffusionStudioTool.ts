// NOTE: Diffusion Studio integration is a stub — not registered in the palette
// because the @diffusionstudio/core API needs verification before this works.

export async function runDiffusionStudio(): Promise<string> {
  throw new Error(
    'Diffusion Studio integration pending — @diffusionstudio/core API verification required.'
  )
}
